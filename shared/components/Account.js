import React, { Component, PropTypes } from 'react';

const Account = () => {

  render() {
    return(

    );
  }
}

Account.PropTypes = {
  authed: PropTypes.object.isRequired
}

export default Account;
